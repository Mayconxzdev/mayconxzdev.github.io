name: Validate and deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install resume dependencies
        run: python -m pip install --disable-pip-version-check reportlab pypdf
      - name: Generate current ATS resumes
        run: python scripts/generate_resumes.py
      - name: Validate resumes
        run: python scripts/validate_resumes.py
      - name: Validate static pages and references
        run: python scripts/validate_site.py
      - name: Validate JavaScript syntax
        run: node --check js/site.js
      - name: Upload generated site
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

  deploy:
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
